#include "cruxpass.h"
#include <stdlib.h>

int authentication(void *master_passi) {
  /* [TODO:]
   * hash the passd str
   * cmp it with the saved passd hash
   * if correct use the password to decrypt db
   */
  return EXIT_SUCCESS;
}
